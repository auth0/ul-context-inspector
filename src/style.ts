// Aggregated style imports so consumers automatically get CSS side effects
// when importing anything from the library entry point.
// Contains compiled Tailwind (prefixed) + minimal custom styles.
import './lib/tailwind.css';
import './lib/styles.css';
